# 카카오 톡딜 상세페이지 작업, 이렇게 하면 돼요

> 에이전트에게 말로 시켜서 작업하는 방법을 쉽게 정리한 문서예요.
> 기술적인 건 에이전트가 다 할거니, 명령만 하시면 돼요.

---

## 먼저, 이게 무슨 일이냐면

카카오 톡딜에 올릴 **상품 모아보기 페이지**를 만드는 일이에요.

- 상품이 카드처럼 쭉 나열되고, 카드를 누르면 그 상품 상세 화면으로 넘어가요.
- 이걸 **회차별로** 만들어요. 회차는 그냥 진행 날짜예요.
  예를 들어 `260903`은 2026년 9월 3일에 진행하는 묶음이에요.

디자이너가 할 일은 딱 하나예요.
**"뭘 어떻게 고쳐줘"** 하고 에이전트한테 말로 시키는 거예요. 나머지 기술적인 건 에이전트가 처리해요.

---

## 0단계. 시작 전 준비 작업 (현업 파일 정리 + 사진 넣기)

에이전트한테 "만들어줘" 하기 전에, 디자이너가 먼저 해두는 밑작업이에요.
크게 두 가지예요.

### (1) 현업이 준 엑셀을 "표 형태"로 정리하기

현업한테 받은 파일은 그대로 쓰기 어려운 경우가 많아요.
에이전트가 읽기 좋게 이런 모양으로 정리해줘요.

- **맨 첫 줄(첫 행)** 에는 **필드명(제목)만** 쭉 넣어요.
  예: `순서 | 년도 | 시즌 | 모델번호 | 모델_컬러 | 컬러코드 | 품목명 | 택가 | 톡딜가 | 혜택가 | 최종할인율`
- **그 아래 줄부터** 실제 상품 데이터가 한 줄에 하나씩 쫙 깔리게 해요.

```
순서  년도  시즌  모델번호      모델_컬러         컬러코드  품목명        택가     톡딜가   혜택가   최종할인율
1     2026  FW   OD261ILS02   OD261ILS02_BLK   BLK      경량 자켓      99,000   79,000   69,000   30
2     2026  FW   OD253ILS07   OD253ILS07_OLI   OLI      플리스 집업    89,000   69,000   59,000   34
...
```

- 이렇게 "첫 줄=제목, 그 아래=데이터"로 깔끔하게 만들어두면,
  에이전트가 이 표를 읽어서 카드를 자동으로 만들 수 있어요.
- 정리한 엑셀은 `_작업소스/{회차}/기초데이터/` 폴더에 넣어둬요.

> 표 정리도 어려우면 받은 파일을 주면서 "이거 첫 줄에 필드명, 아래에 데이터로 정리해줘"
> 라고 에이전트한테 시켜도 돼요.

### (2) 사진을 폴더 규칙에 맞게 넣어두기

사진은 두 종류이고, 넣는 곳과 이름 규칙이 서로 달라요.

**① 상품 누끼컷 (현업한테 받은 것)** — 카드에 보이는 상품 바닥컷
- 넣는 곳: `_작업소스/{회차}/누끼컷&사은품/`
- 이름 규칙: `순서.모델코드_컬러.png` (예: `12.OD263ILS09_GRY.png`)
- 뒷면/착장 등 한 카드에 2장이면 두 번째는 `-1` 붙임 (예: `12-1.OD263ILS09_GRY.png`)
- 사은품 누끼컷은 `_작업소스/{회차}/누끼컷&사은품/사은품/` 안에

**② 상품 상세 이미지 (피그마에서 작업한 것)** — 카드 눌렀을 때 뜨는 긴 상세 화면
- 넣는 곳: `_작업소스/{회차}/상세이미지/`
- 이름 규칙: `순서_모델코드.jpg` (예: `67_OD243BPT08.jpg`)
  - ⚠️ 누끼컷과 규칙이 조금 달라요. 누끼컷은 `12.모델_컬러`, 상세는 `67_모델코드` (점이 아니라 밑줄, 컬러 없음)
- 이 상세 이미지는 최종적으로 **서버의 `detail_image/` 폴더**로 올라가요. (뒤 "서버 어디에 올리나요" 참고)

> 정리하면: **누끼컷 = 카드용(누끼컷&사은품 폴더), 상세 = 피그마 작업물(상세이미지 폴더 → 서버 detail_image).**
> 파일 이름 앞의 숫자는 둘 다 "구성(카드) 번호"라서, 번호만 잘 맞으면 서로 연결돼요.

---

## 작업은 딱 두 종류뿐이에요

### 종류 1. 새 회차를 처음 만들 때 (회차당 딱 한 번)

새 회차가 시작되면 상품 목록이 담긴 **엑셀**하고 **상품 사진**을 준비해요.
이렇게 말하면 돼요.

> "260920 회차 새로 시작할게. 엑셀이랑 누끼컷 준비됐어. 페이지 만들어줘."

그럼 에이전트가 엑셀을 읽어서 카드들을 **한꺼번에 자동으로** 쫙 만들어요.
이건 **회차당 딱 한 번만** 하는 작업이에요.

### 종류 2. 이미 만든 걸 고칠 때 (제일 자주 하는 일)

만들고 나서 "이 부분 바꿔줘" 하는 요청이에요. 예를 들면:

> "구성 12번 상품명을 이걸로 바꿔줘"
> "구성 30번 가격 틀렸어, 정상가 89,000원으로 고쳐줘"
> "구성 7번에 컬러 하나 추가해줘"
> "구성 4번은 NEW 딱지 빼줘"

이때는 처음부터 다시 만드는 게 아니라, **그 카드 하나만 콕 집어서** 고쳐요.

---

## ⚠️ 여기가 제일 중요해요 (사고 안 나려면)

**한번 만든 다음부터는, 엑셀로 다시 만들면 안 돼요.**

왜냐면요:

- 처음 엑셀로 만든 뒤에, 우리는 손으로 이것저것 고쳐왔어요.
  (상품명 다듬고, 가격 바꾸고, 딱지 빼고…)
- 근데 **엑셀로 처음부터 다시 만들어버리면, 그동안 손으로 고친 게 전부 사라져요.**
  원점으로 돌아가는 거예요.

그래서 규칙은 간단해요.

> **처음 만들 때만 엑셀. 그 다음부터는 손으로 그 카드만 고친다.**

디자이너가 이걸 외울 필요는 없어요. 그냥 **"구성 몇 번을 이렇게 고쳐줘"** 라고만 말하면,
에이전트가 알아서 엑셀 안 건드리고 그 카드만 고쳐요.
혹시라도 "엑셀로 다시 만들어"라고 하면, 에이전트가
"그러면 그동안 고친 거 다 날아가는데 진짜 그럴까요?" 하고 되물을 거예요.

---

## 폴더는 이렇게 생겼어요 (딱 필요한 것만)

컴퓨터 안 프로젝트 폴더 `8 (카카오 톡딜)` 안은 이렇게 생겼어요.
디자이너가 실제로 신경 쓸 폴더는 **`_작업소스/{회차}/` 안** 이 거의 전부예요.

```
8 (카카오 톡딜)/
│
├── _작업소스/                     ← 여기가 작업 재료 창고
│   ├── 공용에셋/                  로고·배경 (회차 상관없이 공용, 건드릴 일 거의 없음)
│   │
│   ├── 260911/                   ← 회차 폴더 (회차 하나당 폴더 하나)
│   │   ├── 기초데이터/            엑셀·행사내용 (엑셀은 처음 만들 때만 씀)
│   │   ├── 누끼컷&사은품/         ★ 현업이 준 상품 누끼컷(바닥컷) 넣는 곳
│   │   │   └── 사은품/            사은품 누끼컷은 여기
│   │   ├── 메인&카드 캡처결과/    ★ 에이전트가 캡처한 결과 (상단 01~04 + 카드)
│   │   │   └── cards/             완성된 카드 이미지(01.jpg~) → 서버에 올릴 것
│   │   ├── 상세이미지/            ★ 피그마 상세 작업물 → 서버 detail_image/로 업로드
│   │   ├── 상품상세html/          엑셀기반 품번별 상세 html → 서버에 업로드
│   │   ├── 작업용.html            ★ 진짜 원본. 수정은 전부 여기서 일어나요 (에이전트가 함)
│   │   └── main-image.jpg         메인 비주얼 배경
│   │
│   └── 260920/                   다음 회차는 이렇게 새 폴더로
│
├── 미리보기/                      ← 눈으로 확인하는 화면
│   ├── 260911_로컬확인.html       내 컴퓨터 이미지로 미리 확인 (인터넷 없어도 됨)
│   └── 260911_카카오톡딜.html     서버 이미지로 확인 (네틀리파이에 뜨는 것)
│
├── _스크립트/                     에이전트가 쓰는 자동화 도구 (디자이너는 안 건드림)
│
└── 디자이너_가이드.md             ← 지금 읽는 이 문서
```

새 회차를 시작하면 회차 폴더(`_작업소스/{회차}/`) 안에 아래 5개 폴더가 항상 만들어져요.

| 폴더 | 무엇을 넣나 | 누가 채우나 |
|---|---|---|
| `기초데이터/` | 상품 엑셀, 행사내용 메모 | 디자이너 (현업 파일 정리) |
| `누끼컷&사은품/` | 상품 누끼컷(바닥컷) + `사은품/` 하위폴더 | 디자이너 (현업한테 받은 것) |
| `메인&카드 캡처결과/` | 상단 4장 + `cards/` (카드 이미지) | 에이전트 (캡처 자동) |
| `상세이미지/` | 피그마에서 만든 품번별 상세 이미지 | 디자이너 (피그마 작업물) |
| `상품상세html/` | 엑셀 기반으로 자동 생성되는 품번별 html | 에이전트 (자동 생성) |

> ⚠️ 폴더 이름은 마음대로 바꾸지 마세요. 특히 회차 폴더(`260911` 같은 숫자)와
> 위 5개 폴더 이름이 바뀌면 스크립트·서버 주소랑 안 맞아서 화면이 깨져요.

---

## 받은 파일은 어디에 넣나요

새 회차를 시작할 때, 받은 재료를 이 자리에 넣으면 돼요.

| 받은 것 | 넣는 곳 | 파일 이름 규칙 |
|---|---|---|
| 상품 누끼컷 (현업) | `_작업소스/{회차}/누끼컷&사은품/` | `순서.모델코드_컬러.png` (예: `12.OD263ILS09_GRY.png`) |
| 상품 뒷면/착장 사진 | `_작업소스/{회차}/누끼컷&사은품/` | 같은 번호에 `-1`을 붙임 (예: `12-1.OD263ILS09_GRY.png`) |
| 사은품 누끼컷 | `_작업소스/{회차}/누끼컷&사은품/사은품/` | 그대로 |
| 상품 상세 이미지 (피그마) | `_작업소스/{회차}/상세이미지/` → 서버 `detail_image/` | `순서_모델코드.jpg` (예: `67_OD243BPT08.jpg`) |
| 상품 목록 엑셀 | `_작업소스/{회차}/기초데이터/` | 첫 줄=필드명, 아래=데이터 |

- 앞의 **숫자가 구성(카드) 번호**예요. `12.`으로 시작하면 12번 카드에 들어가요.
- 뒷면 사진처럼 한 카드에 사진이 2장이면, 하나는 `12.`, 다른 하나는 `12-1.`로 넣으면
  카드 안에 두 장이 나란히 들어가요.
- 파일 이름만 규칙대로 맞춰서 폴더에 넣어두고, 에이전트한테
  "260920 누끼컷 다 넣었어. 페이지 만들어줘" 라고 하면 돼요.

> 회차 폴더 자체를 아직 안 만들었으면, 그냥 "260920 회차 폴더 만들어줘"라고 해도 돼요.
> 폴더 만드는 것도 에이전트가 해줘요.

---

## 반영은 어떻게 되나요 (재료 → 화면까지)

받은 재료가 실제 카드 화면이 되기까지 흐름이에요. **가운데 두 단계는 에이전트가 해요.**

```
① 누끼컷 + 엑셀 넣기            ← 디자이너가 함
        │
        ▼
② 작업용.html 만들기/고치기      ← 에이전트가 함 (자동)
        │
        ▼
③ 카드 이미지(jpg)로 뽑기        ← 에이전트가 함 (자동)
   → 메인&카드 캡처결과/cards/ 에 저장됨
        │
        ▼
④ 미리보기로 눈 확인            ← 디자이너가 함
        │
        ▼
⑤ 서버(S3)에 올리기            ← 이 단계까지 하면 카카오에 반영
```

즉 디자이너는 **①(재료 넣기) → ④(확인) → ⑤(올리기)** 만 하면 되고,
가운데 만드는 과정은 "만들어줘 / 고쳐줘" 한마디면 에이전트가 처리해요.

---

## 서버 어디에 올리나요

완성된 카드 이미지(`메인&카드 캡처결과/cards/` 안의 jpg들)를 아래 서버 자리에 올려요.

- **서버(S3) 위치** (예: 260911 회차는 폴더명이 `2609_2_kakaotalk_deal`)
  ```
  aws-childy-image 버킷 / img/outdoor2026/promotion/{회차 서버폴더}/card/
  ```
- **인터넷 주소로는**
  ```
  https://img.childy.kr/img/outdoor2026/promotion/2609_2_kakaotalk_deal/card/01.jpg
  ```
- 회차마다 서버 폴더 이름이 달라요(`2609_2_kakaotalk_deal` 등). 어느 폴더에 올릴지는 에이전트한테 물어보면 알려줘요.

서버 폴더는 이렇게 나뉘어요.

- (폴더 바로 밑) — 상단 공통 이미지 `01_main_visual.jpg` `02_kakao_channel.jpg` `03_benefits.jpg` `04_tokdeal_benefits.jpg`
- `card/` — 카드 썸네일 이미지 (`01.jpg` ~) ※ 폴더명이 **card**(단수)
- `detail_image/` — 카드 눌렀을 때 뜨는 상세 이미지 (`순서_모델코드.jpg`, 피그마 작업물)
- `kakao_detail/` — 카드 눌렀을 때 열리는 상세 페이지 html (`순서_모델코드.html`)

무엇을 올리냐면 상황에 따라 달라요. **헷갈리면 에이전트한테 "이번에 올릴 이미지 목록 알려줘" 하면 딱 집어줘요.**

- **새 회차 처음 올릴 때**: 상단 4장 + 카드(`card/`) + 상세 이미지(`detail_image/`) + 상세 html(`kakao_detail/`) 전부.
- **카드만 고쳤을 때**: 바뀐 카드 이미지만 `card/` 에 올려요.
- **상단(메인/사은품/톡딜혜택)만 고쳤을 때**: 바뀐 상단 이미지만(예: `03_benefits.jpg`) 폴더 바로 밑에 올려요.
- **상세 이미지(피그마)만 고쳤을 때**: 바뀐 상세 이미지만 `detail_image/` 에 올려요.
- 내용이 안 바뀐 건 다시 안 올려도 돼요.

### 파일질라(FileZilla)로 올리는 순서

우리는 **파일질라**라는 프로그램으로 **드래그해서** 올려요. 순서는 이래요.

1. **파일질라를 켜고 서버에 접속해요.**
   - 위쪽에 호스트/사용자명/비밀번호를 넣고 [빠른 연결] 을 누르거나,
     저장해둔 접속(사이트 관리자)에서 우리 서버를 골라 연결해요.
   - 접속 정보 (한 번 넣어두면 다음엔 저장된 걸 고르면 됨):
     - 호스트: `○○○`
     - 사용자명: `○○○`
     - 비밀번호: `○○○`
     - 포트: `○○○`
   - (아직 안 채워져 있으면, 회사에서 받은 접속 정보를 위에 적어두세요.)

2. **화면은 왼쪽 = 내 컴퓨터, 오른쪽 = 서버 예요.**

3. **왼쪽(내 컴퓨터)** 에서 이 폴더로 이동해요.
   ```
   _작업소스/{회차}/메인&카드 캡처결과/cards/
   ```
   > 참고: 내 컴퓨터 폴더는 `cards`(복수)지만, 올리는 서버 폴더는 `card`(단수)예요. 이름이 살짝 달라요.

4. **오른쪽(서버)** 에서 올릴 폴더로 이동해요. (회차 서버폴더 예: `2609_2_kakaotalk_deal`)
   - 카드 썸네일이면:
     ```
     img/outdoor2026/promotion/2609_2_kakaotalk_deal/card/
     ```
   - 피그마 상세 이미지면:
     ```
     img/outdoor2026/promotion/2609_2_kakaotalk_deal/detail_image/
     ```
   - 상단 공통 이미지(메인/사은품/톡딜혜택)면 회차 폴더 **바로 밑**:
     ```
     img/outdoor2026/promotion/2609_2_kakaotalk_deal/
     ```
   - (회차가 바뀌면 `2609_2_kakaotalk_deal` 부분만 그 회차에 맞게 이동)

5. **올릴 이미지를 왼쪽에서 오른쪽으로 드래그해요.**
   - 카드 몇 장만 고쳤으면 그 파일들만 골라서 드래그 (예: `12.jpg`, `30.jpg`).
   - 상세 이미지는 `detail_image/`, 카드는 `card/`, 상단 이미지는 폴더 바로 밑 (폴더 헷갈리지 않게).
   - 처음 전체를 올리는 거면 각 폴더에 해당 파일 전부 드래그.

6. **"덮어쓸까요?" 하고 물어보면 → [덮어쓰기](overwrite) 를 선택해요.**
   - 같은 이름 파일을 새 걸로 바꾸는 거라 덮어쓰는 게 맞아요.
   - "이후 항목에도 적용" 같은 체크가 있으면 켜두면 한 번에 처리돼요.

7. **아래쪽 전송 목록이 다 끝나고 실패(빨간색)가 없는지 확인해요.**

올린 뒤:

- 예전 이미지가 잠깐 보일 수 있어요 → **세게 새로고침(Cmd+Shift+R)** 하거나 조금 기다리면 새 걸로 바뀌어요.
- `미리보기/{회차}_카카오톡딜.html` 을 열어서 실제 올라간 모습을 확인하면 끝.

> ⚠️ 오른쪽(서버)에서 폴더를 지우거나 이름을 바꾸지 마세요. 올리기(드래그)만 해요.

---

## 고쳐달라고 할 때 요령

**"구성 번호"를 꼭 같이 말해주세요.**

- ⭕ 좋은 예: "**구성 30번** 가격을 79,000원으로 바꿔줘"
- ❌ 아쉬운 예: "가격 79,000원짜리를 89,000원으로 바꿔줘"

왜냐면 같은 상품명이나 같은 가격이 여러 카드에 겹쳐 있을 수 있어요.
번호 없이 "이 가격 바꿔줘" 하면, 엉뚱한 카드가 같이 바뀔 수 있어요.
**번호로 콕 집어주면** 그 카드만 정확히 고쳐요.

그리고 **말한 것만 고쳐요.** "구성 12번 상품명 바꿔줘" 하면 12번 상품명만 손대고,
다른 카드나 다른 항목은 안 건드려요.

---

## 고친 다음 확인은 이렇게

에이전트가 카드를 고치면, **이미지로 다시 뽑아서** 미리보기 화면을 만들어줘요.
디자이너는 그 미리보기를 브라우저로 열어서 **눈으로 확인**만 하면 돼요.

> "고쳤어요. 미리보기 열어서 12번 카드 확인해보세요."

이런 식으로요. 마음에 안 들면 다시 "여기 이렇게 더 바꿔줘" 하면 돼요.

---

## 다 됐으면 실제 반영 (업로드)

확인이 끝나면, 고친 카드 이미지를 **실제 서버에 올려야** 카카오에서 보여요.

- 보통은 **바뀐 카드 이미지만** 올리면 돼요.
  (상세 화면 이미지는 안 바뀌었으면 그대로 둬요.)
- 올린 뒤엔 화면에 예전 이미지가 잠깐 보일 수 있어요.
  그럴 땐 **새로고침을 세게(Cmd+Shift+R)** 하거나 조금 기다리면 새 이미지로 바뀌어요.

> 이 "업로드" 부분은 실제로 어떤 방법으로 올리는지(웹에서 드래그로 올리는지, 프로그램을 쓰는지)에 따라
> 방법이 달라요. 쓰는 방식을 알려주면, 클릭 순서까지 여기에 채워넣을게요.

---

## 그래서, 딱 이것만 기억하세요

1. **준비/재료 넣기** —
   ⓐ 엑셀은 "첫 줄=필드명, 아래=데이터"로 정리해서 `기초데이터/` 폴더에.
   ⓑ 상품 누끼컷은 `누끼컷&사은품/`에 `순서.모델코드_컬러` 규칙으로 (사은품은 `누끼컷&사은품/사은품/`).
   ⓒ 피그마 상세 이미지는 `상세이미지/`에 `순서_모델코드.jpg` 규칙으로 (서버 `detail_image/`행).
2. **만들기 / 고치기** — 에이전트한테 말로 시키기.
   새 회차는 "만들어줘", 수정은 "구성 몇 번을 이렇게 고쳐줘"(번호 꼭!).
3. **확인** — `미리보기/` 폴더의 확인용 화면을 브라우저로 열어 눈으로 체크.
4. **올리기** — 파일질라로 접속해서, 왼쪽 `메인&카드 캡처결과/cards/` 의 카드 이미지를
   오른쪽 서버 `.../{회차}_kakaotalk_deal/card/` 로 드래그(덮어쓰기)하면 카카오에 반영.
   (상세 이미지는 `상세이미지/` → 서버 `detail_image/`, 상세 html은 `상품상세html/` → 서버 `kakao_detail/`)

---

## ✅ 올리기 전에 꼭 체크 (상품 수가 바뀐 회차는 특히)

상품을 추가하거나 뺀 회차는, 한 군데만 바뀌고 다른 데가 옛날 그대로 남는 실수가 잦아요.
올리기 전에 에이전트한테 **"올리기 전에 상품 수랑 미리보기 다 맞는지 확인해줘"** 라고 한 번 시키면 돼요.
확인 항목은 이거예요.

1. **상품 수가 다 맞나요?** — 카드 개수, 상세 화면 개수, 미리보기 개수, 그리고 메인 목록 페이지의
   "N개 구성" 숫자가 **전부 같은 수**인지.
2. **미리보기가 새 걸로 바뀌었나요?** — 아래 세 개가 모두 새 상품 수로 갱신됐는지.
   - `미리보기/{회차}_로컬확인.html` (내 컴퓨터에서 확인용)
   - `미리보기/{회차}_카카오톡딜.html` (**인터넷 미리보기 = 네틀리파이에 뜨는 것**)
   - 메인 목록 페이지(`index.html`)의 그 회차 칸 "N개 구성" 숫자
3. **카드 눌렀을 때 엉뚱한 상세로 안 가나요?** — 카드와 상세 화면 연결이 맞는지.

> 특히 2번을 자주 놓쳐요. 카드만 새로 만들고 "인터넷 미리보기"를 안 바꾸면,
> 네틀리파이에는 옛날 개수가 그대로 떠요. 상품 수가 바뀌면 이 셋을 꼭 같이 갱신.

가운데 기술적인 건 다 에이전트가 해요.
디자이너는 **재료 넣고 → "몇 번 카드를 어떻게" 말하고 → 확인하고 → 올리기**, 이 네 가지만 하면 돼요.
